# CORS Allowed Origins (Comma-separated values)
# Use "*" to allow all origins in development, or set specific domains in production
ALLOWED_ORIGINS=*,https://reviewsense.vercel.app,http://localhost:3000

# Optional server configuration parameters
PORT=8000
HOST=127.0.0.1
